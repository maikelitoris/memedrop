```
# Dependencies
.dart_tool/
.build/
.pub/

# Logs and temp files
*.log
*.tmp
*.swp

# Environment
.env
.env.local
*.env.*

# Editors
.vscode/
.idea/
*.iml
*.ipr
*.iws

# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Icon?
```

Wait, I need to reconsider. Looking at the input again, there's only one modified file which is `.gitignore` itself and `lib/widgets/sealed_container.dart`. Since this appears to be a Dart/Flutter project based on the file path, but there are no build artifacts, dependency directories, or temporary files in the changes, I should not add Flutter-specific ignores unless there are actual Flutter build artifacts present.

Actually, looking more carefully - the changes show `.gitignore` was modified along with a Dart file, but no build artifacts or dependency directories are listed in the added/deleted files. This suggests it's just source code changes.

```

```